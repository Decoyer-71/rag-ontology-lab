# CLAUDE.md

이 폴더는 **RAG · 온톨로지 학습 실습실**이다. 데이터 분석 작업공간(`C:\projects\oil_DA`)과 **별개 프로젝트**이며, 그쪽에서 검증된 작업 규율만 「학습」이라는 목적에 맞게 이식해 왔다.

**목적은 둘이다.**

1. 사용자가 **RAG와 온톨로지의 메커니즘을 손으로 짜서 이해하는 것**
2. 그 결과물을 **기업 지원용 포트폴리오로 제출 가능한 수준**으로 GitHub에 등재하는 것

⚠⚠ **1번이 본체다.** 코드가 다 채워졌는데 사용자가 왜 그렇게 되는지 설명하지 못하면 **실패한 것이다.** 그리고 그 상태는 포트폴리오로도 실패한다 — 면접에서 첫 질문에 무너진다.

⚠⚠ **2번은 1번의 부산물이지 별도 작업이 아니다.** 포트폴리오용 장식을 따로 만들지 않는다. §13 이 그 기준선이다.

---

## 0. 이식 출처와 미확정 가정

### 0-1. 무엇을 왜 가져왔는가

이 문서는 `C:\projects\oil_DA\CLAUDE.md` 를 **벤치마킹**해 만들었다. 나중에 "이 규칙 왜 있지?" 를 다시 묻지 않기 위해 남긴다.

| 이식함 | 이유 |
|---|---|
| §5 sub-agent 위임 규율 (원문 덤프를 메인이 안지 않는다) | **도메인 무관하게 유효.** 여기선 코퍼스 원문·검색 후보 40건·논문 본문·라이브러리 문서가 덤프 후보다 |
| §4 「진실의 원천」 + `✅`/`⚠`/`⚠⚠` 근거 등급 | 학습에서 특히 중요하다. **"보통 이렇게 한다"는 기억을 측정값처럼 말하는 것**이 이 도메인의 최대 오염원이다 |
| §5-D 기준선 대조 규율 | RAG 튜닝은 **좋아진 지표 하나만 보고하는 병**이 교과서적으로 나타난다. 교본 §1-6 함정 01 이 정확히 그것이다 |
| §5-G 메인 세션 대량 집필 금지 | 여기선 **더 강한 형태**로 바뀐다 → §5-T |
| 실패 기록 보존 원칙 | "이 방법을 썼더니 이렇게 됐고 왜 폐기했는가" 가 같은 삽질을 막는 유일한 장치. **포트폴리오에서는 이게 차별점 그 자체다**(§13) |
| 지뢰 목록 (§6) | **거의 비어 있는 채로 시작**하고 밟을 때마다 추가한다 |
| 존댓말 규율 | 사용자가 같은 사람이다. 작업공간마다 말투가 달라지는 것은 이상하다 |

| 이식 안 함 | 이유 |
|---|---|
| 데이터 누수 하드룰 (§5-E) | 지도학습 모델을 학습시키지 않는다. **다만 「골든셋으로 튜닝하고 그 골든셋으로 평가」는 같은 병**이라 §5-E' 로 축소 이식했다 |
| `guard_rawdata` (대용량 원본 차단) | 코퍼스가 작다(문서 15건). 대신 **`guard_spoiler`** 로 「아직 안 푼 단계의 정답 열람」을 차단한다 |
| `lib/reportkit` 전체 | 과하다. `src/labkit/report.py` 로 최소 빌더만 둔다 |
| 비용 판정표의 라벨링·도메인 지식 항목 | 무관. 대신 §9 에 **「사용자 시간」과 「조달 용량」** 을 넣었다 |

**⚠ 새로 만든 것 (oil_DA 에 대응물이 없는 것)** — §5-T **대필 금지 하드룰**, §7 **학습 단계 진행 절차**, §10 **커리큘럼**, §13 **포트폴리오 합격선**. 첫째가 이 프로젝트 고유의 실패 모드이고, 나머지는 사용자 요구사항이다.

### 0-2. ⚠⚠ 미확정 가정 — 새 세션은 **첫 턴에 이것부터 확인한다**

2026-09-10 환경 구축 시점에 사용자에게 물었으나 **답을 받지 못한 채 기본값으로 진행한 것들**이다. 아래는 **결정이 아니라 가정**이다.

| # | 항목 | 넣어둔 기본값 | 바꾸면 무엇이 바뀌나 |
|---|---|---|---|
| A1 | **코퍼스 도메인** | **한빛텔레콤 가상사례** (교본이 처음부터 끝까지 쓴 그 사례) | `data/synthetic/` 전체와 골든셋·개념사전·트리플이 전부 교체된다. 후보였던 대안: 석유·에너지 도메인(oil_DA 업무 전이용) |
| A2 | **실습 강도** | **Stage 1~5 는 빈칸 채우기**(시그니처·독스트링·테스트 제공) **→ Stage 6~11 은 백지**(명세와 테스트만) | `src/raglab/*.py` 의 뼈대 분량이 달라진다 |
| A3 | **Stage 9 신경망 임베딩 조달 시점** | Stage 8까지 numpy로 끝낸 뒤 판단 | 지금 하면 **약 2~3GB 조달**이 선행한다(§9). D 여유 324GB — 용량은 더는 제약이 아니다 |

⚠ **확인 전까지 이 가정들을 「사용자가 정한 것」처럼 말하지 마라.** 첫 턴에 세 줄로 묻고 답을 받은 뒤 이 표를 **결정으로 교체**한다(§5-K').

⚠ **원래 「부록(선택)」이던 신경망 임베딩·실데이터 이식은 포트폴리오 요구가 들어오면서 본편 Stage 9·10 으로 올라갔다.** 이유는 §13-2 에 있다.

---

## 1. 디렉터리 구조

```
D:\projects\rag-ontology-lab\
├── CLAUDE.md                      ← 이 파일 (학습 규율)
├── README.md                      ← ⭐ GitHub 정면. 사용자가 아니라 채용 담당자용 (§13)
├── .venv\                         ← ⚠ 커밋 안 함. Python 3.12.13 (§2)
├── .claude\
│   ├── settings.json              ← 훅 등록 + 권한 규칙
│   ├── agents\                    ← 위임용 sub-agent 4종
│   │                                 concept-explainer / code-coach / dataset-scout / verify
│   ├── skills\                    ← next-step · hint · checkpoint · viz-build · repo-publish
│   ├── hooks\                     ← ⭐ guard_handson (대필 차단) · guard_spoiler · guard_claim
│   ├── memory\                    ← ⚠ **이동용 사본이다.** Claude 가 실제로 읽는 곳은
│   │                                 `%USERPROFILE%\.claude\projects\...` → `docs/SETUP.md`
│   └── state\progress.json        ← ⭐ 진도 상태. 훅과 스킬이 이 파일을 읽는다
├── docs\
│   ├── CURRICULUM.md              ← 12단계 전체 설계도
│   ├── PROGRESS.md                ← 사람이 읽는 진도표 + 막힌 지점 기록 (§13 재료)
│   ├── PORTFOLIO.md               ← 포트폴리오 합격선 체크리스트
│   ├── GLOSSARY.md                ← 용어 사전 (학습자 수준 풀이)
│   ├── SETUP.md                   ← 새 PC 셋업 체크리스트
│   └── stages\NN-*.md             ← 단계별 강의. ⚠ **학습 세션이 채운다.** 지금은 뼈대만
├── data\
│   ├── DATA_CARD.md               ← 출처·라이선스·한계·답할 수 없는 질문
│   ├── make_corpus.py             ← 합성 코퍼스 생성기 (재현용. 결과도 커밋함)
│   ├── fetch_korquad.py           ← 실데이터 다운로드 (⚠ 결과는 커밋 안 함)
│   ├── synthetic\                 ← ⚠⚠ **전부 합성이다** (§3-S)
│   │   ├── docs\*.md              ←   한빛텔레콤 문서 15건
│   │   ├── triples.csv            ←   지식그래프 (주-술-목)
│   │   ├── concepts.yaml          ←   개념사전 (동의어·상하위)
│   │   ├── golden_set.yaml        ←   RAG 채점표
│   │   └── competency_questions.yaml ← 온톨로지 채점표
│   └── external\                  ← ⚠ 커밋 안 함. KorQuAD 등 외부 데이터
├── src\
│   ├── raglab\                    ← ⭐ **사용자가 채우는 곳.** Claude 는 여기 본체를 쓰지 않는다 (§5-T)
│   └── labkit\                    ← 제공 도구. 학습 대상이 아닌 배관(로더·중간결과 뷰어·HTML 빌더)
├── tests\                         ← 단계별 통과 조건. **명세이자 채점표**
├── solutions\                     ← 참조 구현. ⚠ 해당 단계를 통과하기 전엔 열지 않는다 (guard_spoiler)
└── outputs\                       ← HTML 산출물 · 측정 결과
```

**⚠ 경로에 공백을 넣지 마라.** 파일명은 **공백 없는 ASCII 소문자 + 하이픈/언더스코어**.

---

## 2. 환경 실측값 (2026-09-10 실측)

| 항목 | 상태 |
|---|---|
| **작업용 파이썬** | ✅ **`.venv\Scripts\python.exe` — Python 3.12.13** (`uv` 가 받은 관리형 CPython). 모든 코드는 **이 인터프리터로** 돌린다 |
| 시스템 파이썬 | ⛔ **없다.** `python` 은 **Microsoft Store 스텁**이라 부르면 죽는다 |
| **패키지 조달** | `uv` **0.12.12**. ⚠⚠ **PATH 에 안 잡힌다** — 전체 경로로 부른다:<br>`%LOCALAPPDATA%\Microsoft\WinGet\Packages\astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe\uv.exe`<br>추가는 `uv pip install --python D:\projects\rag-ontology-lab\.venv\Scripts\python.exe <패키지>` |
| **venv 패키지** | ✅ 실제 설치분 전부: **numpy 2.5.3** · **pyyaml 6.0.3** · **pytest 9.1.1** (+ colorama · iniconfig · packaging · pluggy · pygments) |
| ⛔ **없는 패키지** | **torch · sentence-transformers · transformers · scikit-learn · scipy · pandas · matplotlib · faiss · chromadb · langchain 은 설치돼 있지 않다.** **있다고 가정하고 코드를 쓰지 마라.** 필요하면 위 `uv pip install` 로 조달하고 §9 로 비용을 판정한다 |
| ⚠ **numpy 2.x** | 메이저가 최신이다. **블로그·LLM 기억 속 관용구가 깨진다**(`np.float_` 삭제 등). §4 대로 **실제로 찍어 보고** 쓴다 |
| git | ✅ 2.55.0.windows.3. 원격 `origin` = https://github.com/Decoyer-71/rag-ontology-lab (**공개**). ✅ 전역 신원 **있다** (2026-09-10 재실측). ⚠⚠ **실제 값을 이 파일에 적지 마라** — 공개 저장소다. 확인은 `git config --global user.email` 로 한다. ⚠ 옛 판은 「전역 신원 없음 → 저장소마다 local 지정」이었다 — **이제 불필요하다** |
| **GitHub CLI** | ✅ **`gh` 2.100.0** (2026-09-10 winget 설치). ⚠ **PATH 에 안 잡힐 수 있다** — 전체 경로: `C:\Program Files\GitHub CLI\gh.exe`. ⚠⚠ **인증·계정·토큰 입력은 사용자만 한다** → `repo-publish` 스킬 |
| 디스크 | ✅ **작업 루트는 D 다.** D 931GB (여유 **324GB**) · C 여유 **66GB** (2026-09-10 재실측). ⚠ 옛 판은 「C 여유 19GB · D 없음」이었다 — **프로젝트가 D 로 옮겨졌다.** 여유가 넉넉해져 **2~3GB 조달이 더는 병목이 아니다**(§9 재판정) |
| 인터넷 조사 | WebSearch·WebFetch 사용 가능 → **`dataset-scout` 에 위임**(§5) |

**호출 규약** — PATH 에 등록하지 않았다. 항상 전체 경로로 부른다:

```bash
.venv/Scripts/python.exe -m pytest tests/ -q
```

---

## 3. 검증 루프 — 싼 것부터

이 프로젝트에서 「맞다」는 **두 겹**이다. 코드가 도는 것과, 사용자가 왜 그런지 아는 것. **둘째가 본체다.**

싼 것부터 쓴다:

1. **구문·실행 오류** — 가장 싸다. 스크립트를 돌리면 즉시 난다
2. **단계 테스트** — `tests/test_stageNN_*.py`. **통과 = 그 단계의 명세를 만족했다**는 뜻이지, 이해했다는 뜻은 아니다
3. **⚠⚠ 이해 확인** — `checkpoint` 스킬. 테스트가 통과한 뒤 **사용자에게 「왜」를 묻는다.** 답하지 못하면 그 단계는 **끝나지 않은 것**이다
4. **기준선 대조** — §5-D'. 개선을 주장하려면 **직전 단계 수치와 나란히** 놓는다
5. **참조 구현 대조** — `solutions/` 와 결과를 맞춰 본다. **가장 마지막에 한다** (guard_spoiler)

### 3-S. ⚠⚠ 합성 데이터 표기 의무

`data/synthetic/` 은 **전부 합성이다.** 한빛텔레콤·요금제·고객·수치 전부 교육용 가상 설정이며 실재하지 않는다(교본 말미의 사례 안내와 같다).

- **산출물 최상단에 배너로 표기한다.** `labkit.report` 에 `synthetic=True` 옵션이 있다
- ⚠⚠ **합성 코퍼스에서 나온 Recall 수치를 「RAG 는 이 정도 성능이다」로 일반화하지 마라.** 내가 심은 구조만 발견된다
- **Stage 10 (KorQuAD 실데이터 이식)이 그 일반화 가능성을 재는 유일한 장치다.** 포트폴리오에 합성 수치만 실리면 §13 미달이다

---

## 4. 작업 규율 — 진실의 원천

**기억·블로그가 아니라 실제 문서·실제 실행 출력으로 대조한다.**

| 등급 | 뜻 | 예 |
|---|---|---|
| `✅측정` | 이 PC 에서 실제로 돌려 나온 값 | "Recall@5 = 0.72 (골든셋 25문항 중 18)" |
| `⚠파생` | 측정값에 연산을 가한 것 | "직전 대비 +0.14" |
| `⚠⚠추론` | 측정하지 않고 근거로 미룬 것 | "청킹을 조 단위로 바꾸면 오를 것이다" |
| `⚠2차` | 남의 문서·논문·블로그가 출처 | "교본 §1-6 은 골든셋 부재를 1순위 실수로 꼽는다" |

⚠⚠ **모든 수치에 분모를 붙인다.** "정확도 72%" 만 있고 문항 수가 없으면 **결함**이다.

### 4-1. ⚠⚠ 인용 규율 — 큰따옴표는 **의미가 아니라 글자**를 약속한다

교본·논문·문서를 큰따옴표로 인용할 때는 **원문 글자 그대로**여야 한다. 요약·의역은 큰따옴표를 쓰지 않는다. 이 프로젝트는 교본을 계속 참조하므로 특히 자주 밟는다.

---

## 5. sub-agent 위임 규칙

메인 세션은 원문 더미(코퍼스 전문, 검색 후보 40건, 논문·웹 본문, 라이브러리 문서)를 직접 끌어안지 않는다.

| 대상 | 위임처 |
|---|---|
| **개념 설명 초안** — 학습자 수준에 맞춘 비유·그림 아이디어·오개념 목록 | `concept-explainer` |
| **사용자가 쓴 코드 리뷰** — ⚠ 답이 아니라 **힌트만** 돌려준다 | `code-coach` |
| **공개 데이터·라이선스 조사**, 라이브러리 실태 확인 | `dataset-scout` |
| sub-agent 산출물 검증, 사용자에게 낼 설명·수치 검증 | `verify` |

**⚠ `concept-explainer` 와 `code-coach` 를 혼동하지 마라.** 전자는 *"임베딩이 뭔지 트랜스포머만 아는 사람에게 어떻게 설명하나"*(설명 설계), 후자는 *"이 사람이 쓴 `cosine()` 이 왜 틀렸나"*(진단). 검증 기준이 전혀 다르다.

**위임 스코프 원칙**: agent 1개당 **단일 초점 과업**. 독립 과업은 **분리해 병렬 실행**. 단 같은 파일을 건드리는 과업은 한 agent 에 순차 위임.

**검증**: sub-agent 결론을 **그대로 채택하지 않는다.** `verify` 를 통과시킨 뒤 반영한다.

### 5-T. ⚠⚠⚠ 절대규칙 — **손을 대신 움직이지 않는다** (이 프로젝트의 제1규율)

> **사용자 요구사항 원문:** *"단순 클로드가 코드를 짜주는게 아닌 사용자가 직접 코드를 작성하고 작동 알고리즘을 이해하도록 구성"*

**Claude 는 `src/raglab/` 의 알고리즘 본체를 쓰지 않는다.** 훅 `guard_handson.ps1` 이 기계적으로 차단한다.

#### Claude 가 **해도 되는** 것

- `src/labkit/` — 배관 코드(로더·뷰어·HTML 빌더). **학습 대상이 아니다**
- `tests/` — 통과 조건. **명세를 주는 것은 대필이 아니다**
- `src/raglab/` 의 **시그니처·독스트링·`TODO` 주석·`raise NotImplementedError`** 까지
- `docs/`·`data/`·`README.md`·그림
- **사용자가 쓴 코드를 읽고 「어디가 왜 틀렸는지」 말하기** — 고쳐 주는 것이 아니라 짚어 주는 것
- 사용자가 **명시적으로** "이건 내가 안 짤래, 채워줘" 라고 말한 경우 → 그때만. **그 사실을 `docs/PROGRESS.md` 에 기록한다**

#### Claude 가 **하지 않는** 것

- `TODO` 자리에 동작하는 구현을 채워 넣기
- 사용자가 막혔을 때 **완성 코드를 보여주는 것** → `hint` 스킬로 **단계적 힌트**를 준다
- 채팅에 정답 코드 블록을 띄우고 "복사해 넣으세요" 라고 하기 — **파일에 안 썼을 뿐 대필이다**
- `solutions/` 를 먼저 열어 그 내용을 설명으로 흘리기 → `guard_spoiler`

#### 왜 절대규칙인가

이 프로젝트에서 코드가 다 채워진 상태는 **성공이 아니라 아무것도 아닌 상태**일 수 있다. 대필은 **진행이 빨라 보이기 때문에** 매력적이고, 그래서 규약만으로는 막히지 않는다. 그래서 훅으로 막는다.

⚠⚠ **포트폴리오 관점에서는 더 무겁다.** 대필된 코드는 면접에서 *"이 부분 왜 이렇게 하셨어요"* 한 번에 드러나고, 그 순간 저장소 전체의 신뢰가 무너진다. **§13 의 전제가 §5-T 다.**

### 5-D'. ⚠⚠ 개선은 **기준선과 나란히** 보고한다

교본 §1-6 함정 01 — *골든셋 없이 튜닝한다* — 이 정확히 이 병이다.

- 새 기법을 넣었으면 **넣기 전 수치와 넣은 후 수치를 같은 표에** 놓는다
- **바뀐 지표 전부**를 본다. Recall 이 올랐는데 정밀도가 떨어졌으면 그것도 적는다
- **아무것도 없는 기준선**(무작위 5개, 앞에서 5개)을 반드시 포함한다. **그걸 못 이기는 검색기는 실패한 검색기다**

### 5-E'. ⚠⚠ 골든셋 오염 금지

- **골든셋으로 튜닝하고 같은 골든셋으로 평가하면 그 수치는 낙관 편향돼 있다.** 지도학습의 데이터 누수와 같은 병이다
- 골든셋을 **개발용(dev)/보류용(holdout)으로 나눠 둔다.** 보류분은 단계가 끝날 때만 연다
- ⚠⚠ **성능이 이상하게 좋으면 축하가 아니라 경보다.** Recall@5 가 1.00 이면 먼저 오염을 의심한다

### 5-K'. 개정은 「추가」가 아니라 「교체」다

가정이 결정으로 바뀌면 **표를 고쳐 쓴다.** 옛 판을 남겨 두고 새 줄을 덧붙이지 않는다. 폐기 결정은 그 자리에서 `.claude/state/retired.json` 에 적는다.

---

## 6. 지뢰 목록

밟을 때마다 여기 추가한다. **지금은 환경 구축 시점에 실제로 확인된 것만 있다.**

1. **`python` 은 MS Store 스텁이다.** 반드시 `.venv/Scripts/python.exe` 전체 경로 (§2)
2. **`uv` 는 PATH 에 없다.** 전체 경로로 부른다 (§2)
3. **PowerShell·Bash 에 파이썬 코드를 인라인(`-c "..."`)으로 넣지 마라.** 따옴표·백슬래시가 깨진다. **스크래치패드에 `.py` 로 써서 실행**한다
4. **⚠⚠ 한글이 든 긴 문서를 Bash heredoc 으로 쓰지 마라.** 2026-09-10 에 실제로 깨졌다(`unexpected EOF`). **`Write` 도구를 쓴다**
5. **한글 파일을 읽고 쓸 때 인코딩을 명시한다.** `open(..., encoding="utf-8")`. Windows 기본은 cp949 라 안 적으면 깨진다
6. **`gh` 는 PATH 에 안 잡힐 수 있다.** 전체 경로 `C:\Program Files\GitHub CLI\gh.exe` 로 부른다 (§2). ⚠⚠ **인증은 사용자만** — Claude 는 자격증명을 입력하지 않는다
7. **KorQuAD 는 CC BY-ND 2.0 KR — 재배포 금지.** `data/external/` 은 `.gitignore` 에 들어 있다. **풀지 마라** (`data/DATA_CARD.md`)
8. **⚠⚠ `sed` 로 윈도우 경로(`D:\projects\...`)를 치환하지 마라.** 2026-09-10 에 실제로 깨졌다 — 백슬래시가 `\r`(CR)·`\p` 로 먹혀 `D:projectsag-ontology-lab` 가 나왔다. **백슬래시가 든 치환은 파이썬 스크립트로 한다**(지뢰 3과 같은 뿌리)
9. **pytest 한글 테스트명이 콘솔에서 깨진다.** 윈도우 콘솔이 cp949 라서다. `PYTHONIOENCODING=utf-8` 을 붙이면 정상이다. ⚠ 파이썬 스크립트 자신의 `print` 도 같은 이유로 죽는다 → `sys.stdout.reconfigure(encoding="utf-8")`
10. **⚠ `PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe ...` 처럼 환경변수를 앞에 붙이면 `.claude/settings.json` 의 허용 규칙 `Bash(.venv/Scripts/python.exe *)` 에 안 걸려 권한이 거부된다.** 스크립트 안에서 인코딩을 세우고, 명령은 허용 규칙 모양 그대로 부른다

---

## 7. ⚠⚠ 학습 단계 진행 절차 (핵심 워크플로)

실행 절차는 **`next-step` 스킬**에 있다. 여기엔 왜 그 순서인지를 적는다.

### 7-0. 진도부터 읽는다

`.claude/state/progress.json` 을 읽어 **지금 어느 단계인지** 확인한다. 이걸 안 하면 이미 끝난 단계를 다시 설명하거나 아직 안 배운 개념을 전제로 말하게 된다.

### 7-1. 「이 단계에서 무엇을 할 수 있게 되는가」를 한 문장으로 먼저 말한다

기능이 아니라 **능력**으로 적는다. *"BM25 를 구현한다"* 가 아니라 *"왜 벡터 검색만으로는 요금제 코드 `LTE-S-33` 을 못 찾는지 설명하고, 그 구멍을 메우는 검색기를 짤 수 있다"*.

### 7-2. 직관 → 원리 → 그림 (학습자 수준으로)

사용자는 **트랜스포머 개념까지는 안다. RAG 는 표면적으로만 안다.** 이 두 줄이 모든 설명의 기준선이다.

- **직관** — 수식 없이 한 문장. 비유는 **아는 것에 걸어야 한다** → 트랜스포머·어텐션·토큰은 써도 되고, 벡터DB·리랭커·HNSW 는 **처음 등장 시 풀이가 붙어야 한다**
- **원리** — 최소한의 수식 + **그림 한 장**. 그림이 필요하면 `viz-build` 스킬
- **깨지는 조건** — ⚠⚠ **언제 이 방법이 틀린 답을 내는가.** 이 절을 빼지 마라. §10 원칙 3 대로 **잘 되는 예제만 있는 튜토리얼이 가장 해롭다**

### 7-3. 사용자가 코드를 쓴다 (§5-T)

Claude 는 뼈대와 테스트까지. **본체는 사용자다.** 막히면 `hint` 스킬로 단계적 힌트.

### 7-4. 테스트 → 중간결과를 눈으로 본다

**블랙박스 한 방 호출 금지.** `labkit.inspect` 로 중간 산출물(청크 목록, 벡터 상위 차원, 검색 후보와 점수)을 찍어 본다. **눈으로 본 적 없는 값은 이해한 값이 아니다.**

### 7-5. 기준선과 나란히 측정 (§5-D')

측정 결과는 `outputs/metrics/stageNN.json` 에 남긴다. **이게 쌓인 것이 §13 의 결과 표가 된다.**

### 7-6. ⚠⚠ 이해 확인 — `checkpoint`

테스트가 통과해도 **끝이 아니다.** 「왜」 질문 2~3개에 답해야 그 단계가 닫힌다. 답하지 못하면 그 자리에서 §7-2 로 되돌아간다.

### 7-7. 기록

`docs/PROGRESS.md` 에 한 줄, `.claude/state/progress.json` 에 상태를 적는다. **막혔던 지점과 그 이유**를 반드시 남긴다 — 그게 나중에 README 의 재료가 된다(§13-3).

### 7-8. 말투

**모든 사용자 응답은 존댓말로 한다.** `CLAUDE.md`·스킬·코드 주석 같은 내부 규율 문서는 평서체다.

---

## 8. 산출물 규격 — HTML

최종 산출물은 **HTML 파일 하나**다(`outputs/`). 규격은 oil_DA §8 을 그대로 따른다:

| 항목 | 규격 |
|---|---|
| **자기완결** | CSS·JS·그림 전부 **파일 안에** (그림은 인라인 SVG 또는 base64) |
| **오프라인** | 외부 CDN·폰트·이미지 요청 **금지** |
| **테마** | 밝음/어두움 양쪽 (`prefers-color-scheme`) |
| **반응형** | 넓은 표·그림은 자기 컨테이너 안에서 가로 스크롤. 본문은 가로 스크롤 금지 |
| **한국어** | 본문 한국어 · 존댓말 · 전문용어는 **첫 등장 시 괄호로 원어** |
| **구조** | ① 한눈 요약 ② 문제 정의 ③ 데이터 ④ 방법(직관→원리→가정) ⑤ 결과 ⑥ **한계와 오용 경고** ⑦ 재현 방법 |
| **근거 등급** | §4 등급 표기 + 상단에 범례 |
| **⚠⚠ 합성 표기** | 최상단 배너 (§3-S) |

빌더는 `src/labkit/report.py`. **직접 HTML 문자열을 조립하지 마라.**

---

## 9. 비용·실현가능성 판정표

| 항목 | 조달 | 비용 | 비고 |
|---|---|---|---|
| 합성 코퍼스 생성·확장 | numpy(**설치됨**) + 표준 라이브러리 | **가장 쌈** | ⚠ **내가 심은 구조만 발견된다** |
| TF-IDF · BM25 · 코사인 · RRF | numpy(**설치됨**) | **가장 쌈** | 전부 직접 구현 대상 |
| 그래프 탐색(BFS·다중홉) | 표준 라이브러리 | **가장 쌈** | ⛔ `networkx` 미설치 — 필요 없다 |
| KorQuAD 다운로드 (Stage 10) | `fetch_korquad.py` | 쌈 (약 40MB) | ⚠⚠ **CC BY-ND — 재배포 금지** |
| **신경망 임베딩** (Stage 9) | ⛔ **torch·sentence-transformers 미설치** — `uv` 조달 | **⚠ 약 2~3GB. D 여유 324GB — 용량 제약 아님** | **조달 전 사용자에게 확인한다** |
| 재순위(Cross-Encoder) | ⛔ 미설치 — 위와 같은 스택 | 위와 같음 | Stage 9 를 한 뒤에만 의미가 있다 |
| 벡터 DB(faiss·chroma) | ⛔ 미설치 | 중간 | ⚠ **이 프로젝트에 필요 없다.** 교본 §2-4 — *그래프 DB 는 대부분 필요 없다* 와 같은 판단. **넣으면 오히려 §13 감점**(도구 나열은 이해의 증거가 아니다) |
| LLM 호출 (생성 단계) | API 키 필요 | ⚠ **사용자만 결정 가능** | Stage 8 전에 **모아서 한 번에** 묻는다. ⚠ 없어도 Stage 8 은 성립한다(검색·규칙 검사까지가 본체) |
| **사용자 시간** | 사용자만 가능 | ⚠⚠ **가장 비쌈** | 단계 하나가 몇 시간이면 그 설계는 잘못됐다. **쪼갠다** |

### 판정 원칙

- **가장 단순한 방법으로 이해가 되면 거기서 멈춘다.** 라이브러리는 직접 구현이 끝난 뒤 대조용으로만
- **조달이 큰 단계는 그 자체로 위험 신호다.** 같은 개념을 이미 있는 것으로 가르칠 수 있으면 그쪽
- 사용자에게 물을 것은 **모아서 한 번에** 묻는다 (§12-3)

---

## 10. 커리큘럼

전체 설계도는 **`docs/CURRICULUM.md`**. 여기엔 뼈대만 둔다.

| 단계 | 무엇을 할 수 있게 되는가 | 교본 대응 |
|---|---|---|
| **0** | 채점표부터 만든다 — 골든셋과 역량질문이 왜 코드보다 먼저인지 | RAG STEP 1 · 온톨로지 STEP 1 |
| **1** | 문서를 「구조로」 자르고, 글자수로 자르면 무엇이 깨지는지 측정으로 보인다 | RAG STEP 2·3 |
| **2** | 의미를 숫자 배열로 바꾼다 — TF-IDF 를 numpy 로 직접 | RAG STEP 4 |
| **3** | 가까운 벡터를 찾는다 — 코사인 유사도. **첫 Recall 측정** | RAG STEP 4·5 |
| **4** | 글자로 찾는 검색(BM25)을 붙이고 RRF 로 합친다 — 왜 둘 다 필요한지 | RAG STEP 5 |
| **5** | 틀린 답을 **A·B·C 로 쪼갠다** — 검색 실패/자료 부재/생성 실패 | RAG STEP 7 |
| **6** | 사내 용어를 개념으로 정규화한다 — `concepts.yaml` 한 장의 효과를 측정 | 온톨로지 STEP 1~5 · 붙는자리 ① |
| **7** | 트리플을 걸어간다 — **문서 검색으로는 절대 안 나오는 3홉 질문** | 온톨로지 STEP 4·7 · 붙는자리 ② |
| **8** | 규칙 위반을 검사하고, 전부 합쳐 하나의 파이프라인으로 | 온톨로지 STEP 7 · 붙는자리 ③ |
| **9** | **신경망 임베딩으로 교체** — 트랜스포머가 임베딩이 되는 지점. TF-IDF 대비 개선폭을 잰다 | RAG STEP 4 심화 |
| **10** | **KorQuAD 실데이터로 이식** — 합성에서 나온 결론이 어디까지 살아남는지 | §3-S · §13-2 |
| **11** | **포트폴리오 패키징** — HTML 리포트 · README · 재현 스크립트 · GitHub 등재 | §13 |

원칙 셋 (oil_DA §10 이식):

1. **개념 하나당 돌아가는 코드 하나.** 설명만 있는 학습은 남지 않는다
2. **직접 구현 → 참조 구현 대조.** 라이브러리가 무엇을 대신해 주는지 알아야 그 한계도 안다
3. **틀리는 예제를 같이 만든다.** 언제 깨지는지를 본 사람만 그 방법을 안다. **잘 되는 예제만 있는 튜토리얼이 가장 해롭다**

---

## 11. 현재 상태

| 항목 | 상태 |
|---|---|
| 환경 구축 | ✅ 완료 (2026-09-10). venv · `.claude` · 코퍼스 · 뼈대 · 테스트 |
| 학습 진행 | ⏸ **미착수.** 사용자 지시로 새 세션에서 시작 |
| §0-2 가정 확인 | ⛔ **미확인.** 새 세션 첫 턴 과업 |
| `docs/stages/*.md` | ⛔ **뼈대만.** 각 단계 강의는 그 단계에 도달했을 때 쓴다 |
| 측정 결과 | ⛔ **없음.** `outputs/metrics/` 비어 있음 — §13 결과 표를 아직 못 채운다 |
| GitHub 등재 | ✅ **완료** (2026-09-10) — [github.com/Decoyer-71/rag-ontology-lab](https://github.com/Decoyer-71/rag-ontology-lab) · **공개** · `main` · 커밋 7개 |

---

## 12. ⚠⚠ 보고 규율

oil_DA §12 를 그대로 이식한다. **보고는 작업의 부산물이 아니라 산출물이다.**

### 12-1. 결론 먼저. 과정은 기본 비공개
메인 판단 → `verify` 반박 → 정정의 **왕복은 보고하지 않는다. 최종만 전한다.**

### 12-2. 규약 번호와 전문용어에는 **한 줄 풀이**를 붙인다
*"§5-T 에 걸립니다"* 가 아니라 *"§5-T(제가 대신 코드를 짜지 않는다는 규칙)에 걸립니다"*.

### 12-3. 한 번에 하나만 묻는다
확인이 여러 개면 **모아서 한 번에**(§9).

### 12-4. 길이 — 기본 보고 **15줄 이내**
단계 강의는 예외다. 그건 산출물이지 보고가 아니다.

### 12-5. ⚠⚠ 자기서사 금지
*"제가 처음엔 이렇게 생각했는데"*, *"앞선 판이 틀려서"* 는 쓰지 않는다. **사용자는 배우러 온 사람이지 내 작업 과정을 감사하러 온 사람이 아니다.**

### 12-6. ⚠⚠ 학습자를 평가하지 않는다
틀린 코드를 짚을 때 **코드를 말하고 사람을 말하지 않는다.** *"이해를 못 하셨네요"* 가 아니라 *"이 줄에서 분모가 0이 될 수 있습니다"*.

---

## 13. ⚠⚠ 포트폴리오 합격선 (사용자 요구: 2026-09-10)

> **사용자 요구사항 원문:** *"학습수준은 기업지원에 포트폴리오로 제출할정도면 좋겠어"*

체크리스트는 `docs/PORTFOLIO.md`. 여기엔 **판정 기준**을 적는다.

### 13-1. 독자는 60초 안에 판단한다

채용 담당자·현업 리드는 README 를 **끝까지 읽지 않는다.** 그래서:

- **README 첫 화면에 「무엇을 만들었고 수치가 얼마인가」가 있어야 한다.** 설치 방법이 첫 화면에 오면 실패다
- **결과 표 하나 + 아키텍처 그림 하나**가 스크롤 없이 보여야 한다
- 나머지는 그 아래로

### 13-2. ⚠⚠ 차별점은 「돌아간다」가 아니라 「측정하고 한계를 안다」다

RAG 튜토리얼 저장소는 널려 있다. **LangChain 몇 줄로 돌린 데모는 변별력이 0이다.** 이 저장소가 다른 점은 셋뿐이다:

1. **직접 구현** — TF-IDF·BM25·RRF·코사인·그래프 탐색을 라이브러리 없이 짰다 → *메커니즘을 안다*는 증거
2. **⚠⚠ 단계마다 기준선 대조 측정** — 각 기법이 Recall 을 얼마나 올렸는지 **숫자로** 있다 → *튜닝을 미신으로 하지 않는다*는 증거 (§5-D')
3. **⚠⚠ 합성 → 실데이터 이식으로 한계를 잰다** (Stage 10) → *자기 결과를 과대평가하지 않는다*는 증거. **이 항목이 가장 희소하다**

⚠ **1·2 만 있고 3 이 없으면 「잘 만든 튜토리얼」이고, 3 이 있으면 「분석을 할 줄 아는 사람」이다.** 그래서 Stage 10 을 선택 부록에서 본편으로 올렸다(§0-2).

### 13-3. ⚠⚠ 실패 기록을 지우지 마라 — 그게 차별점이다

`docs/PROGRESS.md` 의 **막힌 지점·폐기한 접근·틀렸던 가설**은 README 의 「배운 것」 절로 올린다.

- *"청킹을 글자수 512로 했더니 요금표가 잘려 숫자 답변이 전부 틀렸다. 조 단위로 바꾸니 Recall@5 가 0.48 → 0.71"* ← **이런 한 줄이 이력서 한 장보다 강하다**
- 매끈하게 성공만 적힌 저장소는 **읽는 사람이 안 믿는다**

### 13-4. 코드 품질 최저선

포트폴리오는 코드를 읽힌다. 사용자가 직접 쓰되(§5-T), **다음은 만족해야 한다** — Claude 는 이걸 **리뷰로 지적**한다(고쳐 주는 것이 아니라):

| 항목 | 기준 |
|---|---|
| 타입 힌트 | 공개 함수 시그니처에 붙는다 |
| 독스트링 | **무엇을 하는지**가 아니라 **왜 그렇게 하는지**. 인자·반환·예외 |
| 테스트 | `tests/` 전부 통과. **깨지는 조건 테스트를 포함**한다(§10 원칙 3) |
| 재현성 | `python -m raglab.run_all` 한 방으로 모든 수치가 재생성된다. **시드 고정** |
| 의존성 | `requirements.txt` 가 실제와 일치한다 |
| 커밋 | 단계별로 쪼개진 의미 있는 메시지. **한 방 커밋 금지** |

### 13-5. ⚠⚠ 부풀리지 않는다

- **합성 데이터 수치를 실세계 성능처럼 쓰지 마라** (§3-S)
- **안 쓴 기술을 README 기술스택에 적지 마라.** 면접에서 물어본다
- **"프로덕션 레디" 같은 말을 쓰지 마라.** 이 저장소는 학습 실습실이고, 그렇게 적는 것이 더 신뢰를 얻는다
- 「한계」 절을 반드시 둔다 — §8 구조 ⑥. **빼면 그 문서는 위험하다**
